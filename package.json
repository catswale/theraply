{
  "name": "root",
  "private": true,
  "dependencies": {
  },
  "scripts": {
    "web": "lerna --scope=therapist-web run start --stream --",
    "expo": "lerna --scope=mobile-app run start --stream --",
    "postinstall": "cd ./packages/mobile-app && expo-yarn-workspaces postinstall"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": ["**/react-native", "**/react-native/**", "**/expo", "**/expo/**"]
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "typescript": "^4.0.3",
    "metro-config": "^0.56.0"
  }
}
